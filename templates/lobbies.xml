<fest:template xmlns:fest="http://fest.mail.ru" context_name="lobbies">
    <div class="new-lobby-box">
        <div class="new-lobby-box__error js-create-error"> Lobby already exists! </div>
        <div class="new-lobby-box__header">NEW LOBBY</div>
        <form class="new-lobby-box__form">
            <input class="new-lobby-box__input" placeholder="Lobby name" required="required" />
            <input class="new-lobby-box__submit" type="submit" value="CREATE"/>
        </form>
        <a href="/#lobbies" class="button-back">
            BACK
        </a>
    </div>

    <div class="lobby-box">
        <div class="lobby-box__header">AVAILABLE LOBBIES</div>
        <div class="lobby-box__outer-table">
            <div class="lobby-box__inside-table">
                <table class="lobby-table">
                    <thead>
                        <tr>
                            <th>NAME</th>
                            <th>PLAYERS</th>
                            <th> </th>
                        </tr>
                    </thead>
                    <fest:each iterate="lobbies" index="i" value="top">
                        <tr>
                            <td class="lobby-table__name"><fest:value>lobbies[i].name</fest:value></td>
                            <td><fest:value>lobbies[i].count</fest:value>/<fest:value>lobbies[i].maxCount</fest:value></td>
                            <td><a href="#" class="lobby-table__join">JOIN</a></td>
                        </tr>
                    </fest:each>
                </table>
            </div>
        </div>
        <div class="lobby-box__refresh"><a href="#" class="lobby-box__refresh_link js-refresh">REFRESH</a></div>
        <div class="lobby-box__create"><a href="#" class="lobby-box__create_link">CREATE LOBBY</a></div>
        <a href="#" class="button-back js-back-main">BACK</a>
    </div>
</fest:template>